//Padding
import 'package:flutter/material.dart';
import 'package:get/get.dart';

class AppPaddings {
  static EdgeInsets mainPadding = EdgeInsets.symmetric(
      horizontal: Get.width * 0.05, vertical: Get.height * 0.03);
  static EdgeInsets registerPadding = EdgeInsets.symmetric(
      horizontal: Get.width * 0.06, vertical: Get.height * 0.028);
  static EdgeInsets defaultPadding = EdgeInsets.symmetric(
      horizontal: Get.width * 0.06, vertical: Get.height * 0.025);
  static EdgeInsets accountPadding = EdgeInsets.symmetric(
      horizontal: Get.width * 0.07, vertical: Get.height * 0.03);
  static EdgeInsets defaultPaddings = EdgeInsets.symmetric(
      horizontal: Get.width * 0.04, vertical: Get.height * 0.02);
  static EdgeInsets homePaddings = EdgeInsets.symmetric(
      horizontal: Get.width * 0.04, vertical: Get.height * 0.01);
  static EdgeInsets horizontal = EdgeInsets.symmetric(
    horizontal: Get.width * 0.05,
  );
  static EdgeInsets horizontalForget = EdgeInsets.symmetric(
    horizontal: Get.width * 0.03,
  );
  static EdgeInsets vertical = EdgeInsets.symmetric(
    vertical: Get.height * 0.02,
  );
  static EdgeInsets verticalStore = EdgeInsets.symmetric(
      vertical: Get.height * 0.03
  );
}
